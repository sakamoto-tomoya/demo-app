# security.md

## 1. 目的
このドキュメントは、レストラン予約システムにおける個人情報とシステム全体のセキュリティ方針を定義する。  
顧客情報を安全に管理し、不正閲覧、不正利用、漏えい、改ざんを防ぐことを目的とする。

---

## 2. 基本方針
- 個人情報は必要最小限のみ取得する
- 個人情報は必要最小限のみ表示する
- 個人情報は必要最小限のみ保存する
- 個人情報をログや不要なレスポンスに含めない
- 権限のないユーザーは他人の予約情報を閲覧できないようにする
- 運用ルールと実装ルールの両方で保護する

---

## 3. 取得する個人情報

### 取得対象
- 氏名
- 電話番号
- メールアドレス
- 予約日時
- 人数
- 注文メニュー
- 備考欄の記載内容

### 原則として取得しない情報
- 生年月日
- 住所
- クレジットカード番号
- 不要なプロフィール情報
- マイナンバー等の高機密情報

---

## 4. 個人情報の利用目的
取得した個人情報は、以下の目的に限定して利用する。

- 予約受付
- 予約確認連絡
- リマインド通知
- キャンセル対応
- 来店確認
- 店舗運営上必要な連絡
- 予約履歴管理

上記以外の目的で利用しない。

---

## 5. 保存ルール

### 5-1. 最小保存
- 必要な項目だけ保存する
- 不要な自由入力欄は増やさない
- 備考欄は必要最小限にする

### 5-2. 保存先
- 個人情報は認可されたデータベースに保存する
- フロントエンドのローカルストレージに機密情報を保存しない
- APIレスポンスで不要な個人情報を返さない

### 5-3. 保持期間
- 予約終了後の保持期間を定義する
- 一定期間経過後は削除または匿名化できる仕組みを持つ
- 通知履歴・監査ログは個人情報最小限で保存する

---

## 6. 表示ルール

### 6-1. 顧客側
- 本人の予約情報のみ表示する
- 予約確認画面では必要な内容のみ表示する

### 6-2. 店舗側一覧画面
- 一覧では必要最小限の情報のみ表示する
- 電話番号とメールアドレスは原則マスキングする
- 詳細画面でのみ必要に応じて完全表示する

### 6-3. 管理者画面
- 権限のある管理者のみ個人情報へアクセス可能
- CSV出力や一括出力は権限者のみに限定する

---

## 7. 権限管理

### 7-1. 権限区分
- 顧客
- スタッフ
- 管理者

### 7-2. 権限方針
#### 顧客
- 自分の予約のみ確認可能
- 自分の予約のみキャンセル可能

#### スタッフ
- 店舗の予約一覧を確認可能
- 必要な範囲で予約詳細を確認可能
- システム設定変更は不可

#### 管理者
- 全予約の管理が可能
- メニュー設定、営業時間設定、通知設定が可能
- 権限管理が可能

---

## 8. 通信・認証

### 8-1. 通信
- すべての通信は HTTPS を前提とする
- HTTP通信は本番環境で許可しない

### 8-2. 認証
- 管理画面には認証を必須とする
- 強いパスワードポリシーを適用する
- 必要に応じて二段階認証を検討する

### 8-3. パスワード
- 平文保存は禁止
- 安全なハッシュ化を行う
- 初期パスワードの使い回しを避ける

---

## 9. ログ・監査

### 9-1. ログの原則
- 個人情報をサーバーログに出さない
- console.log に顧客情報を出さない
- エラーログに機密情報を含めない

### 9-2. 監査対象
以下の操作は監査対象とする。
- 予約詳細閲覧
- 予約変更
- キャンセル処理
- 個人情報の更新
- 個人情報の削除
- 権限変更
- CSV出力

### 9-3. 監査ログの項目
- 実行者
- 実行日時
- 操作内容
- 対象ID
- 結果

---

## 10. 開発ルール

### 10-1. 秘密情報
- APIキーをコードに直書きしない
- 環境変数で管理する
- シークレットをGitに含めない

### 10-2. テストデータ
- 実在の個人情報を使わない
- テスト用のダミーデータを使用する
- 本番データを開発環境へコピーしない

### 10-3. 出力ルール
- AIへの指示では途中確認待ちで止まりすぎず、必要な設計とコードを最終までまとめて提示する
- ただし、本番実行や外部サービス操作が必要な箇所は明確に切り分ける

---

## 11. 脆弱性対策
以下を前提として実装する。

- SQLインジェクション対策
- XSS対策
- CSRF対策
- 認可漏れ対策
- セッション管理の適切化
- 入力値バリデーション
- レート制限
- 管理画面のアクセス制限

---

## 12. API設計方針
- 必要最小限の情報だけ返す
- 管理者APIと顧客APIを分ける
- 認証・認可チェックを各APIで必須とする
- 個人情報を含むAPIはキャッシュ制御を慎重に扱う

---

## 13. 通知に関するセキュリティ
- メール本文に過剰な個人情報を載せない
- 通知文に機密情報を含めない
- 予約確認用URLには安全なトークンを使う
- トークンは有効期限を持たせる
- 推測困難な文字列を使用する

---

## 14. 削除・匿名化
- 顧客から削除要望があった場合に対応できるようにする
- 法令や運用要件に応じて、削除または匿名化を実施する
- 削除後に残す情報は統計・監査に必要な最小限とする

---

## 15. 禁止事項
- 個人情報をログに出すこと
- 個人情報をURLパラメータにそのまま含めること
- 本番データを開発用に無断利用すること
- 権限なしで全件閲覧できる実装にすること
- APIキーや秘密情報をソースコードに埋め込むこと
- クレジットカード情報を自前DBに保存すること

---

## 16. 将来の強化案
- 二段階認証
- IP制限
- 管理画面へのアクセス監視
- 自動アラート
- WAF導入
- データ暗号化強化
- バックアップ暗号化
- セキュリティヘッダー強化
