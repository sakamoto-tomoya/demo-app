# CLAUDE.md

## プロジェクト概要
このプロジェクトは、レストランの予約受付システムです。
顧客はメニューを選択した後、空いている日付と時間を確認して予約できます。
予約日が近づいた際には、失念防止・キャンセル防止のための通知を送ります。

## 目的
- 顧客が簡単に予約できること
- 空き枠を正確に表示すること
- 予約忘れや無断キャンセルを減らすこと
- 店舗側が予約状況を管理しやすくすること
- 個人情報を安全に管理すること

## 想定機能
- メニュー選択
- 人数選択
- 空き日時表示
- 予約登録
- 予約確認通知
- リマインド通知
- キャンセル処理
- 管理画面での予約一覧表示

## 開発方針
- まずはMVPを優先する
- 予約枠判定ロジックを最優先で正確に実装する
- 通知機能はメールを基本とする
- 日本語UIで分かりやすくする
- 既存機能を壊さない小さな差分で修正する
- 個人情報保護を前提に設計する

## 重要ルール
- メニューごとに所要時間が異なる場合は、その時間を使って空き枠を判定する
- 席数・同時予約上限・営業時間を超える予約は受け付けない
- キャンセル時は該当枠を再び予約可能に戻す
- 通知送信履歴を保持する
- 無断キャンセル判定が必要な場合は来店ステータスを持つ

## 出力ルール
- 回答は日本語
- 変更するファイル名を明記する
- 実装時は貼り付け場所を明記する
- 修正時は原因、修正内容、確認方法をセットで出す
- 可能な限り途中で止まらず、実装完了まで必要な出力を一括で提示する
- run や確認待ちで中断せず、要件定義 → 設計 → 必要ファイル → 実装コード → 確認手順まで通して出力する
- ただし、外部サービスの実行や実環境操作が必要な箇所は、その部分だけ明確に区別して示す

## セキュリティ方針
- 個人情報は最小限のみ取得する
- 氏名、電話番号、メールアドレスなどの個人情報は必要な用途以外で保存しない
- 通信は必ず HTTPS 前提とする
- データベースのアクセス制御を行い、管理者以外は予約情報の個人情報を閲覧できないようにする
- 個人情報をログに出力しない
- 開発用ダミーデータと本番個人情報を混在させない
- 環境変数やAPIキーはコードに直書きしない
- 個人情報を含むレスポンスは必要最小限にする
- 退会・キャンセル後の個人情報は保持期間ルールを決めて削除または匿名化できるようにする
- 管理画面では電話番号やメールアドレスのマスキング表示を基本とする
- パスワードを扱う場合は平文保存を禁止し、必ず安全なハッシュ化を行う
- クレジットカード情報は自前保存しない。必要な場合は外部決済サービスを利用する
- 個人情報の更新・閲覧・削除操作は監査ログ対象とする
- 権限のないユーザーが他人の予約情報を参照できないようにする
- SQLインジェクション、XSS、CSRF など基本的なWeb脆弱性対策を前提とする

## 個人情報の取り扱い
### 取得してよい項目
- 氏名
- 電話番号
- メールアドレス
- 予約日時
- 人数
- 選択メニュー
- 備考欄（必要最小限）

### 原則として避ける項目
- 生年月日
- 住所
- 不要な詳細プロフィール
- 決済カード情報の自前保存

## 管理画面の表示ルール
- 一覧画面では個人情報を必要最小限のみ表示する
- 詳細画面以外では電話番号・メールアドレスは一部マスキングする
- CSV出力や一覧出力は権限者のみに限定する
- スクリーンショット共有を前提に、一覧画面では過剰な個人情報を表示しない

## 禁止事項
- 勝手にDB構造を大きく変更しない
- 営業時間や予約ルールをコードに直書きし続けない
- 通知タイミングを複数箇所に分散実装しない
- 空き枠判定を画面ごとに別ロジックで作らない
- 個人情報を console.log やサーバーログに出力しない
- APIレスポンスに不要な個人情報を含めない
- テストコードやサンプルデータに実在の個人情報を使わない
- シークレットキーや認証情報をGitに含めない

## 実装優先順位
1. メニュー・営業時間・席数などの基本データ設計
2. 空き枠判定ロジック
3. 予約登録機能
4. 個人情報を考慮した認証・権限設計
5. 予約一覧・管理画面
6. 通知機能
7. キャンセル処理
8. 来店管理・通知履歴管理
9. 削除・匿名化・監査ログなどのセキュリティ強化
